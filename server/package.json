{
  "name": "@tinybrowser/gateway",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "node --test dist/*.test.js",
    "build": "tsc -p tsconfig.json && esbuild ../packages/runtime/src/entry.ts --bundle --format=iife --platform=browser --target=es2022 --outfile=dist/tinybrowser-runtime.js && mkdir -p dist/demo && cp ../apps/playground/public/demo/index.html dist/demo/index.html",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^24.6.0",
    "tsx": "^4.20.5",
    "esbuild": "0.25.12",
    "typescript": "^5.9.3"
  }
}
